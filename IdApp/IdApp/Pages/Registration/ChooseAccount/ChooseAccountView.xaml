<?xml version="1.0" encoding="UTF-8"?>
<registration:RegistrationStepView xmlns="http://xamarin.com/schemas/2014/forms" 
                                   xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                                   xmlns:registration="clr-namespace:IdApp.Pages.Registration;assembly=IdApp"
                                   xmlns:resources="clr-namespace:IdApp;assembly=IdApp"
                                   x:Class="IdApp.Pages.Registration.ChooseAccount.ChooseAccountView">
    <ContentView.Content>
        <Grid Margin="{StaticResource DefaultMargin}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" HorizontalOptions="Center" VerticalOptions="Center" FontSize="Body" Margin="{StaticResource DefaultMargin}">
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="{x:Static resources:AppResources.ToConnectToDomainYouNeedAnAccount1}"/>
                        <Span Text=" "/>
                        <Span Text="{Binding Path=DomainName}" FontAttributes="Bold"/>
                        <Span Text=". "/>
                        <Span Text="{x:Static resources:AppResources.ToConnectToDomainYouNeedAnAccount2}"/>
                    </FormattedString>
                </Label.FormattedText>
            </Label>
            <StackLayout Grid.Row="1" Orientation="Vertical" Margin="{StaticResource DefaultLeftRightAndBottomMargin}" VerticalOptions="Start">
                <Label Text="{x:Static resources:AppResources.Account}" FontSize="Body"/>
                <Entry Text="{Binding Path=AccountName}"/>
            </StackLayout>
            <ActivityIndicator Grid.Row="2" IsVisible="{Binding Path=IsBusy}" IsRunning="{Binding Path=IsBusy}" 
                               VerticalOptions="Center" Margin="{StaticResource DefaultMargin}" Color="{StaticResource AccentColor}"/>
            <Button Grid.Row="3" Text="{x:Static resources:AppResources.CreateAccount}" Command="{Binding Path=CreateNewCommand}" 
                    Margin="{StaticResource DefaultMargin}" />
            <Button Grid.Row="4" Text="{x:Static resources:AppResources.ScanInvitation}" Command="{Binding Path=ScanQrCodeCommand}" 
                    Margin="{StaticResource DefaultMargin}" />
        </Grid>

    </ContentView.Content>
</registration:RegistrationStepView>